name: CI/CD Pipeline DevOps

on:
  push:
    branches: [main]

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Installer dependances
        run: pip install flask pytest
      - name: Lancer les tests
        run: pytest app/test_app.py -v

  build:
    name: Build et Push Docker
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build et Push
        uses: docker/build-push-action@v4
        with:
          context: ./app
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/flask-app:latest

  deploy:
      - name: Deployer sur Kubernetes
        run: |
          kubectl apply -f k8s/deployment.yaml --insecure-skip-tls-verify
          kubectl apply -f k8s/service.yaml --insecure-skip-tls-verify
          kubectl rollout status deployment/flask-app --insecure-skip-tls-verify
